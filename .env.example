# Server
PORT=8080
DATA_DIR=./data
AUTH_PASSWORD=change-me
AUTH_TOKEN=generate-a-long-random-string
GOOGLE_PLACES_API_KEY=

# Client (build-time, baked into the JS bundle — only public-safe values)
VITE_MAPTILER_KEY=
